<h1>Builds</h1>
<% @builds.each do |build| %>
  <article>
    <h2><%= build.icon_emoji %> <%= build.title %></h2>
    <% if build.description.present? %>
      <p><%= build.description %></p>
    <% end %>
    <span><%= build.status %></span>
  </article>
<% end %>
