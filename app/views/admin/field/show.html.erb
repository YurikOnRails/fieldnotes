<h1><%= @series.title %></h1>
<%= link_to "Edit", edit_admin_field_path(@series) %>
<%= link_to "Back", admin_field_index_path %>
<h2>Items</h2>
<% @series.field_items.ordered.each do |item| %>
  <p><%= item.kind %> — <%= item.caption %></p>
<% end %>
